---
import { Icon } from "astro-icon/components";

interface Props {
  icon: string;
  text: string;
  href: string;
  class?: string;
}

const props = Astro.props;
---

<button class={`bg-red-500 text-white text-sm px-2 py-1 rounded-xl hover:bg-opacity-75 transition-colors ${props.class}`}>
  <a href={props.href} class="flex flex-row items-center gap-2">
    <Icon name={props.icon} />
    <span>{props.text}</span>
  </a>
</button>
